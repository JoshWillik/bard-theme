{{!< default}}

{{# author}}
  <h1>Posts by {{name}}</h1>
{{/author}}

{{#foreach posts}}
  <article class="{{post_class}}">
    <h2><a href="{{url}}" class="permalink">{{title}}</a></h2>
    <p>
      {{excerpt words="26"}}
      <a href="{{url}}">Read More</a>
    </p>
  </article>
{{/foreach}}
